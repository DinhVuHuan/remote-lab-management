// Config ứng dụng
export const APP_CONFIG = {
  appName: 'Remote Lab Management',
  version: '1.0.0',
  socketUrl: window.location.origin,
  defaultMachine: '',
  socketTimeoutMs: 12000,
};

export default APP_CONFIG;
